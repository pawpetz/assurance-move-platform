# Copy this file to .env.local and fill in real values for local development.
# In production (Lovable / your hosting provider), set these as environment
# variables in that platform's settings instead of committing a .env file.

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
